version: "3.8"

services:
  proxy-pool:
    build: .
    container_name: proxy-pool
    ports:
      - "2020:8080"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./data:/app/data
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped

